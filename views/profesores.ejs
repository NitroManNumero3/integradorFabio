<%- include('partials/header') %>

<h2 class="mb-4">Profesores</h2>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>DNI</th>
      <th>Teléfono</th>
      <th>Acciones</th>
    </tr>
  </thead>
  <tbody>
    <% profesores.forEach(p => { %>
      <tr>
        <td><%= p.nombre %> <%= p.apellidos %></td>
        <td><%= p.dni %></td>
        <td><%= p.telefono %></td>
        <td>
          <a href="/profesores/<%= p.profesor_id %>/asignaturas" class="btn btn-sm btn-secondary">Ver asignaturas</a>
        </td>
      </tr>
    <% }) %>
  </tbody>
</table>

<%- include('partials/footer') %>
